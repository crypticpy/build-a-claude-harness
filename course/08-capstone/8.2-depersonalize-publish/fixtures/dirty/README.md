# Sample harness (DIRTY fixture)

This is a deliberately un-depersonalized harness sample. Running the check against
this directory must FAIL (exit non-zero) and report the planted leaks in
`api-key.mjs`. See the `clean/` sibling for the depersonalized version.
